import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.xuxuezhou.utopia',
  appName: 'Utopia',
  webDir: 'dist',
  ios: {
    contentInset: 'automatic',
  },
}

export default config
